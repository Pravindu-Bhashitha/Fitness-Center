# FitZone Admin Dashboard

Minimal Next.js admin dashboard to view bookings (read-only).

Run locally:

```bash
cd admin
npm install
npm run dev
```

The admin app expects the backend API to be running on `http://localhost:4000`.
